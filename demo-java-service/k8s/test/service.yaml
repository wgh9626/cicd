apiVersion: v1
kind: Service
metadata:
  name: demo-java-service
  namespace: test
spec:
  selector:
    app: demo-java-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
